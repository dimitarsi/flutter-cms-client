import 'package:flutter/material.dart';

class DynamicField extends StatelessWidget {
  const DynamicField({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Dynamic field");
  }
}
